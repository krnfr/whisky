import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.anetz.whisky',
  appName: 'Whisky',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
